.reservation-item-box
  .reservation-item-box__img
    /それぞれのイメージをとってくる
    = image_tag("#{look.room.pictures.first.content}" ,class: 'reservation-img')
  .reservation-item-box__txts
    %span.reservation-space-name.weight700
      = look.room.intro.title
    %p
      プラン一覧
    .reservation-space-options
      - look.room.plans do |plan|
      .reservation-space-options__item
        = plan.name
    %p
      オプション一覧
    .reservation-space-options
      - look.room.optoins do |option|
        .reservation-space-options__item
          = option
    .reservation-space-addres
      = look.room.space.space_info.access
    .detail-delete-button-box
    -# 個々の下ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー
      = link_to space_room_url(space_id:look.room.space.id ,id:look.room.id ),class:'hover-none' do
        .detail-room-button
          ルームの紹介ページ
