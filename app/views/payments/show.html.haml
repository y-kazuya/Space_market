-# クレカページ

-# = link_to "クレジットカードを追加", new_payments_path

-# %h1
-#   クレジットカード
-# -@cards.each do |card|
-#   %h1
-#     = card.name
-#     = link_to "削除", payments_path(id: card.id), method: :delete, data: { confirm: 'クレジットカードを削除します'}
-#     = link_to "編集", new_payments_path(id: card.id)
-#     -unless card.use?
-#       = link_to "支払い方法に設定する", set_use_payments_path(id: card.id)
-#     - else
-#       支払い方法に設定ちゅう
.credit-card-edit
  .credit-card-edit__title
    クレジットカード
  .credit-card-edit__description
    スペースマーケット利用料の支払いに使用するクレジットカードを登録してください。
  .credit-card-edit__list
    - @cards.each do |card|
      .credit-card-item-box
        %span.weight700.credit-card-item
          %i.fab.fa-cc-visa
        %span.weight700
          **** **** ****
          = card.num_last
        %span.credit-card-item
          名義人:
        %span.weight700
          = card.name
        %span.credit-card-item
          有効期限:
        %span.weight700
          = card.mm
          \/
          = card.yy
        .credit-card-item-box__links
          = link_to new_payments_path(id: card.id) ,class: 'hover-none' do
            %span.green-color.credit-card-item
              カードを編集
          = link_to payments_path(id: card.id),class: 'hover-none', method: :delete, data: { confirm: 'クレジットカードを削除します'} do
            %span.link-light-blue.credit-card-item
              カードを削除
          -unless card.use?
            = link_to set_use_payments_path(id: card.id) ,class: 'hover-none' do
              %span.credit-card-item.link-black
                支払い方法:未選択
          - else
            %span.credit-card-item.red-color
              支払い方法:選択中
  = link_to new_payments_path ,class: 'hover-none' do
    .credit-card-edit__new_button
      クレジットカードを追加
